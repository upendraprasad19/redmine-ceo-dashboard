# Audit Fix Plan — 2026-07-28

## Executive Summary

| Priority | Count | Batch | Est. Files Changed |
|----------|-------|-------|-------------------|
| P0 | 4 | Batch A (immediate) | 8 |
| P1 | 12 | Batch B (pre-deploy) | 18 |
| P2 | 14 | Batch C (this sprint) | 20 |
| P3 | 10 | Batch D (when convenient) | 5 |

---

## Batch A — P0 Critical (Fix Immediately)

### A1: Slack Signature Bypass (AUD-001)
**File:** `pages/api/slack/events.js`
**Change:** Move `let payload` declaration before line 70
**Risk:** Low — single line move
**Test:** Verify signature check runs before event processing

### A2: Missing Email Normalization (AUD-002)
**File:** `pages/api/sync.js`
**Change:** Add `normalizeEmail(u.mail)` at line 134 before DB insert
**Import:** Add `const { normalizeEmail } = require('../../lib/email-utils')`
**Risk:** Low — single function call addition
**Test:** Verify normalized email stored

### A3: No Try/Catch in Auth Endpoints (AUD-003)
**Files:**
1. `pages/api/auth/change-password.js`
2. `pages/api/auth/forgot-password/channels.js`
3. `pages/api/auth/forgot-password/request.js`
4. `pages/api/auth/forgot-password/verify.js`
5. `pages/api/auth/forgot-password/reset.js`
6. `pages/api/profile/prefs.js`

**Change:** Wrap handler body in try/catch, use `send500(res, err, context)`
**Pattern:**
```javascript
export default async function handler(req, res) {
  try {
    // ... existing logic
  } catch (err) {
    return send500(res, err, 'endpoint-name')
  }
}
```
**Risk:** Medium — 6 files, each needs import + wrapper
**Test:** Verify JSON response on DB failure

### A4: Create E2E Test Stubs (AUD-004)
**Files:**
1. `tests/e2e/dashboard-clicks.spec.js`
2. `tests/e2e/dashboard-critical.spec.js`

**Change:** Create minimal Playwright test stubs
**Risk:** Low — new files only
**Test:** `npm run test:e2e:smoke` passes

---

## Batch B — P1 High (Fix Before Next Deploy)

### B1: Morning Briefing Whitelist (AUD-005)
**File:** `pages/api/cron/morning-briefing.js`
**Change:** Add `AND team = ANY(ARRAY['AI','DB','DevOps','JS/UI','Java','QA']::text[])` to lines 104 and 146
**Risk:** Low — query filter addition
**Test:** Verify count excludes non-engineering teams

### B2-B4: Extract Duplicated Constants (AUD-006, 007, 008)
**New File:** `lib/constants.js`
**Change:** Extract statusMap, priorityMap, EXPECTED_TIME_TEAMS to shared constant
**Update Files:**
- `pages/api/sync.js`
- `scripts/sync-redmine.js`
- `scripts/sync-backfill.js`
- `crons/weekly-reconcile.js`
- `pages/api/overview.js`
- `pages/api/timelogs.js`
- `pages/api/no-timelog.js`
- `pages/api/pm-pulse/developer-load.js`
- `pages/api/profile/snapshot.js`
- `components/Dashboard.js`
- `scripts/check-engineering-filter.js`

**Risk:** Medium — 11 files need import updates
**Test:** Gate scripts pass

### B5: Expand APPROVED_PROJECT_IDS Gate (AUD-009)
**File:** `scripts/check-approved-projects-sync.js`
**Change:** Add 8 additional file paths to FILES array
**Risk:** Low — script-only change
**Test:** Gate script detects all 12 copies

### B6: Migrate send500 Bypasses (AUD-010)
**Files:**
1. `pages/api/chat.js`
2. `pages/api/one-on-one.js`
3. `pages/api/sync.js`
4. `pages/api/sync-full.js`
5. `pages/api/sync-leave.js`
6. `pages/api/auth/login.js`

**Change:** Replace `res.status(500).json(...)` with `send500(res, err, context)`
**Risk:** Medium — 6 files
**Test:** Gate script passes

### B7-B9: Add Rate Limiting (AUD-011, 012, 013)
**Files:**
1. `pages/api/auth/forgot-password/request.js` — add `checkRateLimit`
2. `pages/api/auth/forgot-password/verify.js` — add `checkRateLimit`
3. `pages/api/chat.js` — add `checkRateLimit` (30 req/min)
4. `pages/api/sync.js` — add rate limit + timestamp check

**Risk:** Medium — 4 files, need to verify Redis connectivity
**Test:** Rate limit rejects excess requests

### B10: Developer Load Project Filter (AUD-014)
**File:** `pages/api/pm-pulse/developer-load.js`
**Change:** Add `AND project_id IN (SELECT id FROM projects WHERE redmine_id = ANY(...))` to lines 50-51 and 80
**Risk:** Low — query filter addition
**Test:** Verify count only includes approved projects

### B11: Fix err.message Leak (AUD-015)
**File:** `pages/api/cron/run.js`
**Change:** Replace `error: err.message` at line 164 with `error: 'Job failed'`
**Risk:** Low — single line change
**Test:** Response doesn't contain err.message

### B12: Add Security Headers (AUD-016)
**File:** `next.config.js`
**Change:** Add X-Powered-By, Content-Security-Policy, Permissions-Policy, X-XSS-Protection
**Risk:** Low — config-only change
**Test:** Headers present in response

---

## Batch C — P2 Medium (Fix Within Sprint)

### C1: Rate Limiter Fail-Open (AUD-017)
**Decision:** Keep fail-open per vault `[[fail-open-rate-limit]]` — this is by design for auth availability. Document rationale.

### C2: User Existence Leak (AUD-018)
**File:** `pages/api/auth/forgot-password/channels.js`
**Change:** Always return identical response shape regardless of user existence
**Risk:** Low — response normalization

### C3: Middleware Public Path (AUD-019)
**File:** `middleware.js`
**Decision:** Keep `/api/cron` in PUBLIC_PATHS per existing design — each cron endpoint verifies CRON_SECRET independently. Document rationale.

### C4: Expand Gate Script Coverage (AUD-020)
**File:** `scripts/check-approved-projects-sync.js`
**Change:** Add remaining 8 file paths
**Risk:** Low — script update

### C5: sync-backfill Email (AUD-021)
**File:** `scripts/sync-backfill.js`
**Change:** Add email field to INSERT at line 98-121
**Risk:** Low — one-time script

### C6-C8: Module System Standardization (AUD-022, 023, 024)
**Decision:** Defer full migration — too many files (30+) for this sprint. Document as tech debt.

### C9: Fix check-module-system Regex (AUD-025)
**File:** `scripts/check-module-system.js`
**Change:** Fix regex logic at line 59
**Risk:** Low — script fix

### C10: Coverage Threshold CI (AUD-026)
**Decision:** Defer — requires CI workflow creation (not in scope for this fix batch)

### C11: Regenerate Schema Contract (AUD-027)
**Change:** Run schema generation after migration 023
**Risk:** Low — regeneration script

### C12: Remove Latent SQL Injection (AUD-028)
**File:** `lib/gpt-executor.js`
**Change:** Remove dead code at line 58
**Risk:** Low — dead code removal

### C13: Add Unit Tests (AUD-029)
**Files:** 8 lib/ modules
**Decision:** Defer — test creation is significant effort, schedule as separate task

### C14-C15: E2E Test Files (AUD-030, 031)
**Decision:** Partially addressed in Batch A (stubs). Full E2E tests deferred.

---

## Batch D — P3 Low (Fix When Convenient)

### D1: Duplicate console.error (AUD-032)
**File:** `pages/api/sync-leave.js`
**Change:** Remove duplicate line at 99-100
**Risk:** Trivial

### D2-D10: Acceptable Patterns (AUD-033-040)
**Decision:** No action required — these are informational or acceptable patterns.

---

## Execution Order

```
Batch A (P0) — Execute First
├── A1: Slack TDZ fix
├── A2: Email normalization
├── A3: Auth try/catch (6 files)
└── A4: E2E test stubs

Batch B (P1) — Execute Second
├── B1: Morning briefing whitelist
├── B2-B4: Extract constants (lib/constants.js)
├── B5: Expand gate script
├── B6: send500 migration (6 files)
├── B7-B9: Rate limiting (4 files)
├── B10: Developer load filter
├── B11: err.message fix
└── B12: Security headers

Batch C (P2) — Execute Third
├── C1, C3: Document rationale (no code change)
├── C2: User existence leak
├── C4: Gate script expansion
├── C5: sync-backfill email
├── C9: Regex fix
├── C11: Schema contract regeneration
└── C12: Dead code removal
```

---

## Verification Steps

After each batch:
1. Run `npm run audit:gates` — all must pass
2. Run `npm run test:unit` — all must pass
3. Run `npm run lint` — no new violations
4. Update audit closures YAML with terminal states
5. Update vault/INDEX.md if new patterns discovered
